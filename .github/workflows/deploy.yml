name: Build and Deploy Frontend

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/car-frontend:latest

    # - name: Deploy to server
    #   env:
    #     SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    #     SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
    #     SERVER_USER: ${{ secrets.SERVER_USER }}
    #     SERVER_IP: ${{ secrets.SERVER_IP }}
    #     DOCKER_IMAGE: ${{ secrets.DOCKER_USERNAME }}/car-frontend:latest
    #   run: |
    #     mkdir -p ~/.ssh
    #     echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    #     chmod 600 ~/.ssh/id_rsa
    #     ssh-keyscan $SERVER_IP >> ~/.ssh/known_hosts

    #     ssh $SERVER_USER@$SERVER_IP << 'EOF'
    #       docker pull $DOCKER_IMAGE
    #       docker stop car-frontend || true
    #       docker rm car-frontend || true
    #       docker run -d --name car-frontend -p 80:80 $DOCKER_IMAGE
    #     EOF
